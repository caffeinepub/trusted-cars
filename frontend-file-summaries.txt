{
  "frontend/src/pages/CarDetailPage.tsx": "Car detail page with image gallery, full specs, finance section, price card, and action buttons (Book Test Drive, Enquire Now, Call Now). Fixed `flex-shrink-0` → `shrink-0` and removed conflicting `block` class from the Call Now `<a>` element (keeping only `flex`).",
  "frontend/src/components/admin/CarForm.tsx": "Admin modal form for adding/editing cars — fixed to build a `CarInput` object (without `id`) for the add mutation and pass `{ id, carInput }` for the update mutation, matching the backend interface exactly. Also adds an inline error banner inside the form to surface any backend errors visibly to the user.",
  "frontend/src/components/admin/CarManagementTable.tsx": "Admin table listing all cars with thumbnail, specs, price, status/featured badges, and Edit/Mark-as-Sold/Delete actions. Uses the updated CarForm props (`open`, `editCar`, `onClose`) for the edit modal.",
  "frontend/src/components/admin/BookingManagementTable.tsx": "Admin table component displaying all test drive bookings with customer info, car reference, date/time, status badge, inline status update dropdown, and a CSV export button that downloads booking data as a file.",
  "frontend/src/components/CarCard.tsx": "Reusable car listing card showing image, brand/model, specs grid, price/EMI, status badge, and View Details button. Replaced all `flex-shrink-0` with `shrink-0` for Tailwind v3 compliance.",
  "frontend/src/components/Footer.tsx": "Site footer with both the user's custom logo and the Trusted Cars wordmark displayed side by side in the brand section (both inverted for the dark background), quick links, contact info, copyright, and caffeine.ai attribution.",
  "frontend/src/components/Header.tsx": "Sticky header with both the user's custom logo and the Trusted Cars wordmark displayed side by side (user logo on the left, divider, then Trusted Cars logo), plus desktop navigation links, phone call button, and mobile hamburger menu.",
  "frontend/src/components/ImageGallerySlider.tsx": "Image gallery with main view, prev/next navigation, thumbnail strip, zoom button, and lightbox overlay. Replaced `flex-shrink-0` with `shrink-0` on thumbnail buttons.",
  "frontend/src/pages/CarsPage.tsx": "Car listing page with filter sidebar (budget, brand, fuel, transmission, year), responsive grid of CarCard components, and mobile filter toggle. Replaced `flex-shrink-0` with `shrink-0`.",
  "frontend/src/pages/admin/AdminLoginPage.tsx": "Simplified admin login page that removes the debug panel and relies on `loginMutation.isError` for error display. Cleaned up error handling in the submit handler.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend operations — admin login now only verifies credentials client-side (no URL token check), stores `isAdmin` in localStorage, and admin mutations use `useAdminActor` with a `createFreshAdminActor` fallback that uses the canister's built-in access control without requiring a URL token.",
  "frontend/src/pages/admin/AdminDashboardPage.tsx": "Admin dashboard page with header, logout button, and tabbed interface for Car Management (with Add New Car button that opens CarForm modal) and Bookings sections.",
  "frontend/src/hooks/useAdminActor.ts": "Dedicated admin actor hook that initializes the actor when `isAdmin` is set in localStorage, using `createActorWithConfig` and calling `_initializeAccessControlWithSecret(\"\")` for the canister's principal-based access control — no URL token required."
}